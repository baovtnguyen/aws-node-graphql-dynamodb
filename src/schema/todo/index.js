const { TodoTypes } = require('./types');
const { TodoQuery } = require('./query');
const { TodoMutation } = require('./mutation');
const { TodoResolvers } = require('./resolvers');

module.exports = {
  TodoTypes,
  TodoQuery,
  TodoMutation,
  TodoResolvers,
}
